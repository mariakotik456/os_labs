# Лабораторная работа №3

## Управление процессами с использованием Memory-Mapped Files

### Вариант 11

Цепочка процессов: Parent → Child1 → Child2 → Parent

### Компиляция и запуск:

```bash
docker build -t os-lab3 .
docker run -it --rm os-lab3